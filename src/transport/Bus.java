package transport;

public final class Bus extends Transport {

    public Bus(String brand, String model, int year, String country, String colorBody, double maxSpeed) {
        super(brand, model, year, country, colorBody, maxSpeed);
    }
}


